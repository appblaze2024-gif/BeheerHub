rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    /**
     * BEHEERHUB STORAGE RULES
     * Toegankelijk voor alle ingelogde gebruikers voor AI Training en Meldingen.
     */
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}