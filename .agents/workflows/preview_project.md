# Preview Project Workflow

This workflow details how to start the development preview server for this Next.js project.

## Start Development Server
Ensure dependencies are installed, then run the development server. The server will run on `localhost:9002` (as defined in package.json).

```bash
// turbo
npm run dev
```

To view the preview, open `http://localhost:9002` in your browser.
